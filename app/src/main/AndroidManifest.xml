<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SalvadorTech"
        tools:targetApi="31">

        <activity android:name=".AddServicoActivity"
            android:exported="true" />

        <activity
            android:name=".DetalhamentoServicoActivity"
            android:exported="true" />

        <activity android:name=".EsqueceuSenhaActivity"
            android:exported="true" />

        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:exported="true">

        </activity>

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:exported="true" />

        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CadastroFuncionarioActivity"
            android:exported="true" />

        <activity
            android:name=".SalvadorTechActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light">
            <!-- Remover o intent-filter para esta atividade -->
        </activity>

        <activity
            android:name=".CadastroUsuarioActivity"
            android:exported="true">
        </activity>
    </application>
</manifest>
